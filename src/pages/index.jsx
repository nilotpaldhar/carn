import { HomePageTmpl } from '@templates';

/**
 * Render the HomePage component.
 *
 * @return {Element} The HomePage component.
 */
const HomePage = () => <HomePageTmpl />;

export default HomePage;
